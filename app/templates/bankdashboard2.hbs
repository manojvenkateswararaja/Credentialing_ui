{{partial 'head'}}
{{#if showLogin}}
{{/if}}

<div class="container-fluid">
    <br>
    <br>
    <br>
    <br>
    <b>
      <em style="padding:70px 16px">User Request ID with Data Published Date & Time</em></b>
      <br>
      <div class="container">
      <br>
        <table class="table table-bordered">
      <thead>
      <tr>
        <th>REQUEST ID</th>
        <th>DATE</th>
        <th>TIME</th>
        <th>STATUS</th>
      </tr>
       </thead>
      {{#each showrecords   as |item|}}
     
       <tbody>
      <tr>
        <td> <a href="" {{action 'userdetails'  item }}><b style="color:blue">{{item.Records.requestid}} </b></a></td>
        <td> <small  class="text-muted">{{item.Records.date}}</small></td>
        <td> <small  class="text-muted">{{item.Records.time}}</small></td>
        <td> <b style="color:green">{{#if isBankPreclose}}{{status}}{{/if}}</b><b style="color:green">{{item.Records.statusForUser}}{{item.Records.statusForBankLegal}}{{item.Records.statusForCreditRequest}}
         {{item.Records.bankpreclose}}
          <b style="color:tomato;"><a href="" {{action 'postPrecoseRequest'  item }}>{{item.Records.statuspreclose}}</a></b></b></td>
     </tr>
    </tbody>
     {{/each}}
  </table>
            {{!-- <div class="col-xs-3 col-lg-3 col-sm-3">Requestid<a href="" {{action 'userdetails'  item }}><b style="color:blue">{{item.Records.requestid}}    
                    </b>
                    </a></div>
           <div class="col-xs-3 col-lg-3 col-sm-3"><b>Data Published Time</b></div>
               <div class="col-xs-3 col-lg-3 col-sm-3"> Date:<small  class="text-muted">{{item.Records.date}}</small></div>
               <div class="col-xs-3 col-lg-3 col-sm-3">Time:<small  class="text-muted">{{item.Records.time}}</small></div>
         <div class="col-xs-6 col-lg-6 col-md-6 col-sm-6" style="background-color:">
            <div>
               <div style="">status:<b style="color:green">{{#if isBankPreclose}}{{status}}{{/if}}</b>
                  <b style="color:green">{{item.Records.statusForUser}}{{item.Records.statusForBankLegal}}{{item.Records.statusForCreditRequest}}
                      {{!-- {{item.Records.statuspreclose}} --}}
                      {{!-- {{item.Records.bankpreclose}}
                     
                       <b style="color:tomato;"><a href="" {{action 'postPrecoseRequest'  item }}>{{item.Records.statuspreclose}}</a></b>
              
                  </b>
               </div>
            </div>
         </div>
      </div>  --}}
      <br>
     
      </div>
      </div>
      