

<script>
   $(document).ready(function() {
       // Show or hide the sticky footer button
       $(window).scroll(function() {
           if ($(this).scrollTop() > 200) {
               $('.go-top').fadeIn(200);
           } else {
               $('.go-top').fadeOut(200);
           }
       });
       // Animate the scroll to top
       $('.go-top').click(function(event) {
           event.preventDefault();
           $('html, body').animate({
               scrollTop: 0
           }, 300);
       });
      
   });
</script>
   <div id="cont-lock" style="text-align:left ;color:white ">
      <h1><b>Bank Dashboard</b></h1>
   </div>
<div class="dashbord-container">
   <br/>
   <p style="text-align:center;color:#808080 "><b>Total Request</b></p>
      <p class="head-request"><b>ALL Request</b></p>
   <div class="dash-container" >
      <div>
         {{!-- <button  style="text-align:center;margin:67px 0px;color:black" {{action 'toggle'}}>GetRequests</button> --}}
         </div>
        <div class="AllDetails">
         {{#each id as |item|}}
         <ul>
             <div style="background:lightgrey">
            {{!-- reqid<b style="color:#2d8cff ">{{ item.requestid}}</b> --}}
            {{!-- Req ID<a href="" {{action "requestid" item}} > --}}
            Requestid<a href="" {{action 'userdetails' item }}><b style="color:#2d8cff ">{{item.requestid}}</b></a>
               {{!-- Requestid {{action 'userdetails' item}}<b style="color:#2d8cff ">{{ item.requestid}}<br></b>  --}}
            <div class="vl">{{ item.transactionstring.firstname}}</div> 
            <div class="vl2 ">{{ item.transactionstring.propertyType}}</div>
            <div class="vl3 ">{{ item.transactionstring.CompanyName}}</div>
            </div>
            </ul>
            {{/each}}
              {{!-- {{#if isShowingModel}}<br>
              <style>
             table, th, td {
             border: 1px solid grey;
             border-width: thin;
             }
           </style>
              <table style="border:0">
              <tr>
              <th>Request ID</th>
              <th>Name</th>
              <th>puropse of loan </th>
              <th>Need Amount </th>
              <th> Status </th>  
              <th>creditscore</th>
              </tr>
              <tr>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  <td></td>
                  </tr>
              </table>
           {{/if}}   --}}
         
   </div>
   </div>
</div>
<a href="#" class="go-top">Scroll to Top</a>
