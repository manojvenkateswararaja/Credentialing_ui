{{partial 'head'}}
{{#if showDashboard }}
<script>
   $(document).ready(function() {
       // Show or hide the sticky footer button
       $(window).scroll(function() {
           if ($(this).scrollTop() > 200) {
               $('.go-top').fadeIn(200);
           } else {
               $('.go-top').fadeOut(200);
           }
       });
       // Animate the scroll to top
       $('.go-top').click(function(event) {
           event.preventDefault();
           $('html, body').animate({
               scrollTop: 0
           }, 300);
       }); 
   });
</script>
<div id="mySidenav" class="sidenav">
   <a href="javascript:void(0)" class="closebtn" {{action "closeNav"}}>&times;</a>
   <a >Home</a>
   <a {{action "goTonewrequest"}}>User</a>
   <a {{action "goToloan"}}>Bank</a>
   <a {{action "goToproperty"}}>Creditscoregenerator</a>
   <a {{action "goToemployment"}}>LegalPerson</a>
</div>
<div class="container-fluid" style="background-color:lightgrey">
   <h1><b style="margin-left:-2000px">Bank Dashboard</b></h1>
   <br>
   <div class="jumbotron" style="margin-top:-3cm">
      <p style="text-align:center;color:black"><b>TOTAL REQUEST</b></p>
      <div class="container" style="background-color:grey"><b>ALL Request</b></div>
      <br>
      <div class ="container">
         {{#each showrecords   as |item|}}
         <div class="row">
            <div class="col-sm-3">Requestid<a href="" {{action 'userdetails'  item }}><b style="color:blue">{{item.Key}}</b></a></div>
            {{#each item.Record.transactionlist   as |item1|}}
            <div class="col-sm-8">
               <div class="row">
                  <div class="col-sm-3">
                     <div class="vl">Name:{{item1.transactiondetails.firstname}}</div>
                  </div>
                  <div class="col-sm-3">
                     <div class="vl">Properttype:{{item1.transactiondetails.propertyType}}</div>
                  </div>
                  <div class="col-sm-3">
                     <div class="vl">Companyname:{{item1.transactiondetails.CompanyName}}</div>
                  </div>
                  {{!-- 
                  <div class="col-xs-6 col-lg-6 col-md-6 col-sm-6">
                     <div class="vl">status:{{item1.transactiondetails.status}}</div>
                  </div>
                  --}}
               </div>
            </div>
            {{/each}}
         </div>
         <br>
         {{/each}}
      </div>
   </div>
</div>
<a href="#" class="go-top">Scroll to Top</a>
{{/if}}