
{{partial 'head'}}
{{#if showDashboard }}
<script>
   $(document).ready(function() {
       // Show or hide the sticky footer button
       $(window).scroll(function() {
           if ($(this).scrollTop() > 200) {
               $('.go-top').fadeIn(200);
           } else {
               $('.go-top').fadeOut(200);
           }
       });
       // Animate the scroll to top
       $('.go-top').click(function(event) {
           event.preventDefault();
           $('html, body').animate({
               scrollTop: 0
           }, 300);
       }); 
   });
</script>
<div id="mySidenav" class="sidenav">
  <a href="javascript:void(0)" class="closebtn" {{action "closeNav"}}>&times;</a>
  <a >Home</a>
  <a {{action "goTonewrequest"}}>User</a>
  <a {{action "goToloan"}}>Bank</a>
  <a {{action "goToproperty"}}>Creditscoregenerator</a>
  <a {{action "goToemployment"}}>LegalPerson</a>
</div>
   {{!-- <div id="cont-lock" style="text-align:left ;color:black"> --}}
      <h1><b style="margin-left:-2000px">Bank Dashboard</b></h1>
     
   {{!-- </div> --}}
   <br/>
   <div class="dashbord-container" style="background-color:lightgrey">
   <p style="text-align:center;color:black"><b>Total Request</b></p>
      <p class="head-request"><b>ALL Request</b></p>
 
     
    <div class ="container">
         {{#each showrecords   as |item|}}
           <div class="dash-container" style="background-color:bisque"></div>
         <ul>
             <div style="background:lightgrey;margin-bottom:25px">
            Requestid  <a href="" {{action 'userdetails'  item }}><b style="color:black ">{{item.Key}}</b></a>
                </div>
                </ul>
           
                
              {{#each item.Record.transactionlist   as |item1|}}
            <div class="vl">Name:{{ item1.transactiondetails.firstname}}</div> 
          
            <div class="vl2">Properttype:{{ item1.transactiondetails.propertyType}}</div>
            <div class="vl3 ">Companyname:{{ item1.transactiondetails.CompanyName}}</div>
            <div class="vl4">status:{{item1.transactiondetails.status}}</div>
         
             
            {{/each}}
            {{/each}}
            
         </div>
         </div>
<a href="#" class="go-top">Scroll to Top</a>
{{/if}}
