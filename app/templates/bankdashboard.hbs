
{{partial 'head'}}
{{#if showDashboard }}
<script>
   $(document).ready(function() {
       // Show or hide the sticky footer button
       $(window).scroll(function() {
           if ($(this).scrollTop() > 200) {
               $('.go-top').fadeIn(200);
           } else {
               $('.go-top').fadeOut(200);
           }
       });
       // Animate the scroll to top
       $('.go-top').click(function(event) {
           event.preventDefault();
           $('html, body').animate({
               scrollTop: 0
           }, 300);
       });
      
   });
</script>

   {{!-- <div id="cont-lock" style="text-align:left ;color:black"> --}}
      <h1><b style="margin-left:-2000px">Bank Dashboard</b></h1>
   {{!-- </div> --}}
   <br/>
   <div class="dashbord-container" style="background-color:lightgrey">
   <p style="text-align:center;color:black"><b>Total Request</b></p>
      <p class="head-request"><b>ALL Request</b></p>
   <div class="dash-container">
         {{#each id as |item|}}
         <ul>
             <div style="background:lightgrey"> 
            {{!-- Requestid<a href="" {{action 'userdetails' item }}><b style="color:#2d8cff ">{{item.Key}}</b></a>  --}}
             Requestid  <a href=""{{action 'userdetails' item}}><b style="color:#2d8cff ">{{item.Key}}</b></a>
            <div class="vl">{{item.Record.transactiondetails.companyname}}</div> 
            <div class="vl2 ">{{ item.Record.transactiondetails.firstname}}</div>
           <div class="vl3">{{item.Record.transactiondetails.location}}</div> 
           <div class="vl4">{{item.Record.transactiondetails.propertyvalue}}</div>
            </div>
            </ul> <br>
            {{/each}}
         
   
   </div>
</div>
<a href="#" class="go-top">Scroll to Top</a>
{{/if}}